'use client';

/**
 * This component has been simplified as the artifact functionality has been removed.
 * It's kept as a stub for compatibility with existing imports.
 */

export type DataStreamDelta = {
  type: string;
  content: string | any;
};

export function DataStreamHandler({ id }: { id: string }) {
  // No-op component that previously handled artifacts
  return null;
}
